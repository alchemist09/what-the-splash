import { handleStatsLoad } from '../stats_saga'
import { runSaga } from 'redux-saga'
import * as api from '../../api'


describe('Stats Saga', () => {
  test('should load general statistics and handle them if successful', () => {
    
  })
})